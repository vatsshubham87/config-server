server:
  port: 8085

spring:
  application:
    name: Config-Server

  cloud:
   config:
    server:
     git:
      uri: https://github.com/vatsshubham87/microservices-configs/
      clone-on-start: true